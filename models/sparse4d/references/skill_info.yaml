name: sparse4d
network_arch: sparse4d
automl_enabled: true
container_image: tao_toolkit.pyt
data_format: ovpkl
gpu_spec_key: train.num_gpus
actions:
  dataset_convert:
    container_image: tao_toolkit.data_services
    command: annotations convert -e {config_path}
    config_format: yaml
    inputs:
      aicity.root:
        type: folder
        container_path: /data/aicity_root
    outputs:
      results_dir:
        type: folder
    upload_excludes:
    - inputs/
  train:
    command: sparse4d train -e {config_path}
    config_format: yaml
    inputs:
      dataset.data_root:
        type: folder
        container_path: /data/aicity_root/train
      model.head.instance_bank.anchor:
        type: file
      dataset.train_dataset.ann_file:
        type: file
      dataset.val_dataset.ann_file:
        type: file
      dataset.test_dataset.ann_file:
        type: file
    outputs:
      results_dir:
        type: folder
    upload_excludes:
    - inputs/
  quantize:
    command: sparse4d quantize -e {config_path}
    config_format: yaml
    inputs:
      dataset.data_root:
        type: folder
        container_path: /data/aicity_root/train
      model.head.instance_bank.anchor:
        type: file
      dataset.train_dataset.ann_file:
        type: file
      dataset.val_dataset.ann_file:
        type: file
      dataset.test_dataset.ann_file:
        type: file
      dataset.quant_calibration_dataset.images_dir:
        type: folder
    outputs:
      results_dir:
        type: folder
    upload_excludes:
    - inputs/
  evaluate:
    command: sparse4d evaluate -e {config_path}
    config_format: yaml
    inputs:
      dataset.data_root:
        type: folder
        container_path: /data/aicity_root/val
      model.head.instance_bank.anchor:
        type: file
      dataset.train_dataset.ann_file:
        type: file
      dataset.val_dataset.ann_file:
        type: file
      dataset.test_dataset.ann_file:
        type: file
    outputs:
      results_dir:
        type: folder
    upload_excludes:
    - inputs/
  export:
    command: sparse4d export -e {config_path}
    config_format: yaml
    inputs:
      model.head.instance_bank.anchor:
        type: file
    outputs:
      results_dir:
        type: folder
    upload_excludes:
    - inputs/
  inference:
    command: sparse4d inference -e {config_path}
    config_format: yaml
    inputs:
      dataset.data_root:
        type: folder
        container_path: /data/aicity_root/test
      model.head.instance_bank.anchor:
        type: file
      dataset.train_dataset.ann_file:
        type: file
      dataset.val_dataset.ann_file:
        type: file
      dataset.test_dataset.ann_file:
        type: file
    outputs:
      results_dir:
        type: folder
    upload_excludes:
    - inputs/
data_sources: {}
spec_params: {}
key_defaults: {}
spec_shorthand_keys:
  num_epochs: train.num_epochs
  batch_size: dataset.batch_size
  learning_rate: train.optim.lr
description: Sparse4D for multi-camera temporal 3D object detection and tracking. Uses sparse queries with deformable attention
  across camera views and time for end-to-end 3D perception. Includes instance bank for temporal tracking.
