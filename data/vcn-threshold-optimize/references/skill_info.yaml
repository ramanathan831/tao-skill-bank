network_arch: vcn-threshold-optimize
container_image: iva.smart_data_augmentation
actions:
  compute:
    command: python /tmp/user_script.py
    mode: args
    inputs:
      inference-results:
        type: folder
    outputs:
      output-json:
        type: file
    defaults:
      min-recall: 1.0
