{
  "skill_name": "cosmos-anomalygen",
  "evals": [
    {
      "id": 1,
      "prompt": "I have a checkpoint at `results/anomaly_gen/bottle_run/bottle_run_training_FP32_lr0.02_bs=2_2B_512x512` from a previous run. I want to generate 200 synthetic anomaly images for `hairy` and `scratch` defects on the `bottle` texture. Dataset is at `datasets/bottle`. Use 3 search rounds.",
      "expected_output": "Claude sets mode=inference_only, validates checkpoint_dir and step are required, asks for or confirms the step value, then produces the correct shared variable block and phase 0/2-7 command sequence with NUM_SDG=200, NUM_SEARCH_RUN=3, DEFECTS=(bottle+hairy bottle+scratch). Should NOT run Phase 1 (finetune).",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Run the full AnomalyGen pipeline for a new `cable` dataset at `datasets/cable`. Defect spec is at `datasets/cable/defect_spec.jsonl`. I want 150 SDG images. Use 2 GPUs, model size 2b, 2 search rounds. Filter output to nn_score >= 0.7.",
      "expected_output": "Claude sets mode=full, produces the correct shared variable block (NUM_SDG=150, NUM_GPUS=2, MODEL_SIZE=2b, NUM_SEARCH_RUN=2, NN_THRESHOLD=0.7), reads finetune.md and inference.md, and produces commands for all phases 0-7 including Phase 1 training, Phase 7 filter. Should ask for confirmation before writing the training config.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "I trained a model on just `scratch` defects for the `tile` texture. Now I want to add `crack` defects too without retraining. Checkpoint is at `results/anomaly_gen/tile_scratch/tile_scratch_training_FP32_lr0.02_bs=2_2B_512x512`, step 50000. Dataset at `datasets/tile`.",
      "expected_output": "Claude should NOT run the pipeline. It should inspect the checkpoint's ag_config.yaml, recognize that 'crack' is not in the trained defect set, explain the mismatch clearly, and present the three options: (1) mode=full retrain on extended defect set, (2) isolated new model with separate name for just crack, (3) trim spec to only scratch. Should not silently try to run inference with crack.",
      "files": []
    }
  ]
}
